# widget

A small widget library, delivered against a Warrant job.

## Running the tests

Install nothing. From the repository root, run:

    python -m unittest discover -s tests

The suite is offline and takes under a second. A green run means every widget
behaviour described below is covered.

## CHANGELOG

### 0.2.0

- Added the resize handle.
- Fixed the off by one in the grid snap.

### 0.1.0

- First release.

## Notes

This file is a test fixture. It exists so a Warrant job on Testnet Bradbury has
a real, immutable deliverable to judge, fetched from a commit pinned raw URL.
